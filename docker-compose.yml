version: "3.7"

services:
  web:
    image: 2hog/docker-training-samples-micro-sinatra
    init: true
    deploy:
      replicas: 1
    environment:
      AUTH_USER: ${AUTH_USER:-paris}
      AUTH_PASSWORD: ${AUTH_USER:-kasidiaris}
